import React, { FC } from 'react';
import './BlogSide.css';

interface BlogSideProps {}

const BlogSide: FC<BlogSideProps> = () => (
  <div className="BlogSide">
    BlogSide Component
  </div>
);

export default BlogSide;
